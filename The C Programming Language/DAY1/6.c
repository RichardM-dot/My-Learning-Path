#include <stdio.h>
#include <math.h>
#define PI 3.1415926
int main()
{
    int base=2,exponent=3;
    int result=pow(base,exponent);
    printf("%lf,%d",sqrt(8)+sin(30*PI/180),result);
    return 0;
}